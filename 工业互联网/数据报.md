数据报服务（datagram)——每个分组作为一个独立的信息单位传送
* 提供**无连接**的服务，也**无需确认**
* 主机有数据可随时发送
* 每个分组携带**完整的网络地址**（源和目的）——通信效率低
* 每个分组独立进行路由选择，**不保证按序到达**
* 对**故障适应性强**，且易于平衡网络流量。

#### 数据报网络：因特网模型
在网络层没有建立连接过程
一般分组使用的**信宿主机的ID**进行路由选择
同样收发双方的不同分组**经由的路径可能不同**